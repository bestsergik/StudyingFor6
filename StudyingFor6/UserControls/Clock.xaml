<UserControl x:Class="StudyingFor6.UserControls.Clock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyingFor6.UserControls"
             mc:Ignorable="d">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"></ColumnDefinition>
            <ColumnDefinition Width="4"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0"   Height="auto" >
            <Label x:Name="timelbl"  Foreground="Purple" FontSize="28" FontFamily="Stormfaze"  ContentStringFormat="HH:mm" />
        </StackPanel>
        <StackPanel Grid.Column="1"  Orientation="Horizontal">
            <Separator Background="Black" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </StackPanel>
        <StackPanel Grid.Column="2"   >
            <Label x:Name="datelbl"  Foreground="Purple" FontSize="28" FontFamily="Stormfaze" />
        </StackPanel>
    </Grid>
</UserControl>
